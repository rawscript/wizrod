<Window x:Class="Wizrod.HotkeyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wizrod" Width="440" Height="570" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Deactivated="Window_Deactivated" KeyDown="Window_KeyDown">
  <Border CornerRadius="28" BorderThickness="1" BorderBrush="#5CFFFFFF" Padding="18"
          Background="#E02A3B4A" SnapsToDevicePixels="True">
    <Border.Effect><DropShadowEffect BlurRadius="35" ShadowDepth="10" Opacity=".48" Color="#061019" /></Border.Effect>
    <Grid>
      <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
      <StackPanel>
        <DockPanel Margin="3,0,3,16">
          <StackPanel DockPanel.Dock="Left"><TextBlock Text="Wizrod" Foreground="White" FontSize="23" FontWeight="SemiBold"/><TextBlock Text="Your clipboard, on command" Foreground="#B9D3E1" FontSize="12"/></StackPanel>
          <TextBlock DockPanel.Dock="Right" Text="Ctrl + Alt + V" Foreground="#C9E3EF" VerticalAlignment="Center" FontSize="12" />
        </DockPanel>
        <Button x:Name="SearchButton" Style="{StaticResource RoundButton}" Click="Search_Click">
          <StackPanel Orientation="Horizontal"><TextBlock Text="⌕" FontSize="23" Margin="0,-3,12,0"/><TextBlock Text="Search clipboard" VerticalAlignment="Center"/></StackPanel>
        </Button>
        <StackPanel x:Name="SearchPanel" Visibility="Collapsed" Margin="0,-4,0,8">
          <TextBox x:Name="SearchBox" Background="#40FFFFFF" Foreground="White" BorderBrush="#60FFFFFF" Padding="12,8" FontSize="14" TextChanged="SearchBox_TextChanged" />
        </StackPanel>
        <Grid x:Name="Navigation" Margin="0,0,0,10">
          <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition x:Name="FavoritesColumn" Width="*"/></Grid.ColumnDefinitions>
          <Button Grid.Column="0" Style="{StaticResource RoundButton}" Margin="0,0,6,0" Click="Recents_Click" Content="◷  Recents" />
          <Button Grid.Column="1" Style="{StaticResource RoundButton}" Margin="0,0,6,0" Click="Settings_Click" Content="⚙  Settings" />
          <Button x:Name="FavoritesButton" Grid.Column="2" Style="{StaticResource RoundButton}" Margin="0" Click="Favorites_Click" Content="♥  Saved" />
        </Grid>
      </StackPanel>
      <Grid Grid.Row="1">
        <ListBox x:Name="ItemsList" Background="Transparent" BorderThickness="0" Foreground="White" SelectionChanged="ItemsList_SelectionChanged">
          <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,0,0,8"/><Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/><Setter Property="HorizontalContentAlignment" Value="Stretch"/><EventSetter Event="MouseRightButtonUp" Handler="Item_Favorite"/></Style></ListBox.ItemContainerStyle>
          <ListBox.ItemTemplate><DataTemplate>
            <Border Background="#2AFFFFFF" BorderBrush="#3EFFFFFF" BorderThickness="1" CornerRadius="14" Padding="13,10" ToolTip="Click to paste · Right-click to save or remove from Saved">
              <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel><TextBlock Text="{Binding Preview}" TextWrapping="Wrap" MaxHeight="44" Foreground="White" FontSize="14"/><TextBlock Text="{Binding TimeLabel}" Foreground="#B5CEDC" FontSize="11" Margin="0,4,0,0"/></StackPanel>
                <TextBlock Grid.Column="1" Text="★" Foreground="#F9D982" FontSize="15" Visibility="{Binding IsFavorite, Converter={StaticResource BooleanToVisibilityConverter}}" />
              </Grid>
            </Border>
          </DataTemplate></ListBox.ItemTemplate>
        </ListBox>
        <Border x:Name="SettingsPanel" Visibility="Collapsed" Background="#38FFFFFF" CornerRadius="16" Padding="18">
          <StackPanel><TextBlock Text="Settings" FontSize="19" FontWeight="SemiBold" Foreground="White"/><TextBlock Text="Keep copied values for" Foreground="#C7DAE5" Margin="0,16,0,6"/>
            <ComboBox x:Name="RetentionPicker" SelectedIndex="2" SelectionChanged="RetentionPicker_SelectionChanged"><ComboBoxItem Content="1 day"/><ComboBoxItem Content="7 days"/><ComboBoxItem Content="14 days"/><ComboBoxItem Content="30 days"/><ComboBoxItem Content="Forever"/></ComboBox>
            <CheckBox x:Name="FavoritesToggle" IsChecked="True" Content="Show favourites in the menu" Foreground="White" Margin="0,18,0,8" Checked="FavoritesToggle_Changed" Unchecked="FavoritesToggle_Changed"/>
            <TextBlock Text="Tip: click any card to paste it into the active app." Foreground="#B8D1DE" TextWrapping="Wrap" Margin="0,15,0,0"/>
          </StackPanel>
        </Border>
      </Grid>
    </Grid>
  </Border>
</Window>
